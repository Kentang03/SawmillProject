[System.Serializable]
public class LogData
{
    public float length;      // in meters (Panjang tetap)
    public float height;      // in cm (tinggi log akan dipotong dari atas ke bawah)
    public float width;       // in cm (lebar log digunakan untuk validasi)
}